//! `SKILL.md`: re-exported from `godwinmix-protocol`.
//!
//! The frontmatter parser and the check the harness makes live in
//! `godwinmix_protocol::plugin::skill`, so the rule an author is validated
//! against and the rule the index enforces are one piece of code.

pub use godwinmix_protocol::plugin::skill::*;
