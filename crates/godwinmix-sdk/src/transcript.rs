//! Recorded transcripts: re-exported from `godwinmix-protocol`.
//!
//! The format `gmx plugin test --offline` replays lives in
//! `godwinmix_protocol::plugin::transcript`, so a transcript a template ships
//! and a transcript the core replays are read by the same parser.

pub use godwinmix_protocol::plugin::transcript::*;
