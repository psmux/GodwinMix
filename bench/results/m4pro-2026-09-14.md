# Footprint on `m4pro`

| | |
|---|---|
| Machine | `m4pro` |
| CPU | Apple M4 Pro |
| RAM | 24 GB |
| OS | macos aarch64 25.6.0 |
| GStreamer | GStreamer 1.28.7 |
| Commit | `0fbf0ae` |
| Date | 2026-09-14 |
| Canvas | 1280x720 at 30 fps |
| Window | 30 s steady state after warm up |

| Measure | Target | CPU (cores) | RSS (MB) | Other | Verdict |
|---|---|---|---|---|---|
| Core idle RSS and CPU, no sources, no outputs, nobody subscribed | at most 60 MB, at most 1 percent of one core on `pi4` | 0.022 | 58.0 | - | within budget |
| Multiview encoder with no client subscribed | 0, and no pipeline | 0.000 | 0.0 | 0 mosaic pipelines alive | within budget |
| Multiview encoder, one subscriber, 8 fps mosaic 1280 wide | at most 10 percent of one core on `pi5` | 0.025 | 2.8 | 8.0 fps out, 8.0 fps reported | measured |
| Snapshot and motion tracker, while something is asking | at most 5 percent of one core on `pi5`, and 0 when disabled | 0.062 | 19.2 | 0.109 cores for the mosaic and the tracker together, tracker started 1 time(s) | measured |
| Added RSS and CPU per 720p30 file source into the compositor, no encode | at most 40 MB, at most 15 percent of one core on `pi4` | 0.000 | 34.7 | - | within budget |
| 720p30, two live sources, programme encode, software x264 | `pi5` software: at most 2.0 cores; `laptop`: documented | 0.880 | 699.3 | - | measured |
| 720p30, two live sources, programme encode, hardware | `pi4` with hardware encode: at most 1.0 core total; `n100`: at most 0.6 | 0.700 | 5.6 | encoder vtenc_h264_hw | measured |
| Cold start, process exec to first encoded programme frame | at most 2.0 s on `pi4` | - | - | 0.15 s | measured |
| Core binary, one executable, before the platform's GStreamer packages | at most 30 MB, plus about 19 MB of GStreamer | - | - | 8.4 MB | within budget |
| A crossfade between two eight item scenes at 1080p30 | `gpu` and `n100`: no dropped frame; `laptop`: documented | - | - | - | not yet |
| Sixteen hidden slots at alpha 0 | within 2 percent of the no compositor baseline | - | - | - | not yet |

CPU is a fraction of one core over the window. RSS on a row that says "added" is a difference between two readings in one process, taken in the order the rows are printed, so a later row starts from whatever an earlier one did not give back to the operating system. For a clean figure on one row, run it on its own with `--only`.

## What produced each row

### `core-idle`

The programme pipeline, including the H.264 encoder, is built and playing from boot even with no output attached, so this is not an idle process in the sense the budget means. See the note under the table.

```
gmx bench --only core-idle
```

### `multiview-idle`

Asserted, not sampled: there is no mosaic object to cost anything.

```
gmx bench --only multiview
```

### `multiview-subscriber`

```
gmx bench --only multiview-subscriber
```

### `snapshot-tracker`

The delta is against the mosaic on its own, so on a fast machine it can disappear into the noise between two windows. Zero when nothing asks is not a rounding: the follower is not running at all then.

```
gmx bench --only snapshot
```

### `file-source`

In process, not container mode: a sidecar source pays about 41 MB more for its own GStreamer process. This machine decodes in hardware, and on macOS VideoToolbox runs in its own XPC process, so the decode does not appear in this process's CPU at all. The number is the mixer's own cost, not the machine's.

```
gst-launch-1.0 videotestsrc pattern=black is-live=true ! video/x-raw,width=1280,height=720,framerate=30/1 ! compositor name=comp background=black ! video/x-raw,width=1280,height=720,framerate=30/1 ! fakesink sync=true filesrc location=/var/folders/6w/lj67z7ps5vl2tgtzvj78q00m0000gn/T/gmx-bench-720p30.mp4 ! decodebin ! videoconvert ! videoscale ! videorate ! video/x-raw,width=1280,height=720,framerate=30/1 ! comp.
```

### `two-live-sw`

RSS is what this pipeline added to the process, not the process total.

```
gst-launch-1.0 compositor name=comp background=black ! video/x-raw,width=1280,height=720,framerate=30/1 ! videoconvert ! x264enc speed-preset=veryfast bitrate=2500 key-int-max=60 ! h264parse ! fakesink sync=true videotestsrc pattern=smpte is-live=true ! video/x-raw,width=1280,height=720,framerate=30/1 ! comp.sink_0 videotestsrc pattern=ball is-live=true ! video/x-raw,width=1280,height=720,framerate=30/1 ! comp.sink_1
```

### `two-live-hw`

RSS is what this pipeline added to the process, not the process total.

```
gst-launch-1.0 compositor name=comp background=black ! video/x-raw,width=1280,height=720,framerate=30/1 ! videoconvert ! vtenc_h264_hw ! h264parse ! fakesink sync=true videotestsrc pattern=smpte is-live=true ! video/x-raw,width=1280,height=720,framerate=30/1 ! comp.sink_0 videotestsrc pattern=ball is-live=true ! video/x-raw,width=1280,height=720,framerate=30/1 ! comp.sink_1
```

### `cold-start`

Wall clock from exec in the parent. The child's own clock, from the first line of its main to the first encoded frame, read 116 ms; the difference is process start and dynamic linking.

```
gmx bench --only cold-start
```

### `binary-size`

Release build, unstripped.

```
ls -l target/release/gmx
```

### `crossfade-1080p`

scenes do not exist yet, so there is nothing to fade between

```
not yet
```

### `hidden-slots`

scenes do not exist yet

```
not yet
```

