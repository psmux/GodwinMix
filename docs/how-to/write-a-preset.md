# Write a preset

This page moved. Presets exist now, with `gmx preset list|show|apply|save|diff`
and a `preset` kind the loader reads.

* [Make a preset](make-a-preset.md) is the walk through, including
  `gmx preset save`, which writes one from a machine that already works.
* [Presets, the reference](../reference/presets.md) is every manifest key, the
  merge rules, the search order and the six official presets.
* [Your first stream with a preset](../tutorials/first-stream-with-a-preset.md)
  is the timed path from install to on air.
