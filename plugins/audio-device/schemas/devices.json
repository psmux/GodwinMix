{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Audio input discovery",
  "description": "Finding the sound inputs on this machine. There is nothing to set: the operating system is asked each time `discover` is called.",
  "type": "object",
  "properties": {
    "include_monitors": {
      "type": "boolean",
      "title": "Include monitor inputs",
      "description": "PipeWire and PulseAudio present the output of every sound card as an input too, so a machine with four outputs lists four extra devices nobody wants. Off by default.",
      "default": false,
      "examples": [false, true],
      "x-gmx-group": "Advanced"
    }
  },
  "additionalProperties": false
}
